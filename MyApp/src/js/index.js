/*fetch("./json/buttons.json")
    .then(data =>{
        return data.json();
    })
    .then((data) =>{
        let output ="";

        for(let i = 0; i < data.buttons.length; i++){
            output += `<button class="btn btn-primary btn-sm">${data.buttons[i].name} - ${data.buttons[i].id}</button>`
        }

        document.getElementById('buttons').innerHTML = output;
    })


*/
